<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gogo.mapper.MemberMapper">

	<!-- 로그인 -->
	<select id="login" resultType="com.gogo.vo.MemberVO">
		select * from member 
		where memberId = #{memberId}
	</select>
	
	<!-- 권한조회  -->
	<select id="getMemberRole" resultType="String">
		select role_id from memberrole  where memberId = #{memberId}	
	</select>
	
	<!-- 아이디 중복체크 -->
	<select id="idCheck" resultType="int">
		SELECT COUNT(*) FROM member
		WHERE memberId = #{memberId}
	</select>
	
	<!--회원 등록 -->
	<insert id="signup" parameterType="com.gogo.vo.MemberVO">
		INSERT INTO MEMBER (memberId, memberName, BIRTHDAY, pw, memberEmail, GENDER) 
		VALUES (#{memberId}, #{memberName}, #{birthday}, #{pw}, #{memberEmail}, #{gender})
	</insert>
	
	
	<update id="updateNaver">
		update member set snsck = #{snsCk}, profile = #{profile} where memberId = #{memberId}
	</update>
	
	<select id="getPw" resultType="String">
		select pw from member where memberId = #{memberId}
	</select>
</mapper>













